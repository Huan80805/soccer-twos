# Reward Shaping PPO Agent

**Agent name:** RewardShapingPPOAgent

**Author (s):** Update with your name and email

## Description

Packaged PPO policy restored from a Ray RLlib checkpoint trained with `exp_reward_shaping.py` using the custom reward shaping wrapper on the `team_vs_policy` setup.
